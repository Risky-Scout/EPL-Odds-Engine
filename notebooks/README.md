# Notebooks

Keep notebooks lightweight and reproducible.

Recommended pattern:

- notebooks for exploration and presentation
- production logic in `src/`
- outputs saved to `workspace/` or `reports/`
